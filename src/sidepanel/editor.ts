const editor = document.getElementById('editor') as HTMLTextAreaElement

if (editor) {
    editor.addEventListener('input', () => {
        // Text editing functionality - just basic textarea behavior
    })
}

